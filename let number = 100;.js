function circleArea(radius){
    let radiusType = typeof radius;

    if (radiusType === 'string'){
        console.log('We can not calculate the circle area, becase we receive a ${radiustype}')
    }
}

let kek = console.assert("lol");
console.log(kek);